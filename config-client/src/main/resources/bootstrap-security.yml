server:
  port: 8016
spring:
  application:
    name: config-client
  cloud:
    config:
      profile: dev #启用配置后缀名称
      label: dev #分支名称
      uri: http://localhost:8014 #配置中心地址
      name: config #配置文件名称
      username: lwoo
      password: 123456
